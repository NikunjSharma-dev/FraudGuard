# Database Configuration (PostgreSQL with SQLite fallback)
DATABASE_URL=postgresql+asyncpg://frauduser:fraudpass@localhost:5432/frauddb

# Redis Configuration (Redis 7 with in-memory fallback)
REDIS_URL=redis://localhost:6379/0

# Application Configuration
ENVIRONMENT=development
SECRET_KEY=change_me_in_production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8501
